<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WhatToCook.MAUI.ViewModels.Recipes"
             xmlns:model="clr-namespace:WhatToCook.MAUI.Models"
             x:Class="WhatToCook.MAUI.Views.Recipe.RecipeListPage"
             x:DataType="vm:RecipeListViewModel"
             Title="Recipes">

    <Grid Padding="15" RowDefinitions="Auto,*">
        <ActivityIndicator Grid.Row="0"
                           IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           Color="{StaticResource Primary}"/>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Recipes}"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Recipe">
                    <Frame Margin="0,8" Padding="12" BorderColor="LightGray">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="{Binding Title}"
                                   FontSize="18" FontAttributes="Bold"
                                   TextColor="{StaticResource MidnightBlue}"/>
                            <Label Text="{Binding Description}"
                                   FontSize="14"
                                   LineBreakMode="TailTruncation"
                                   MaxLines="2"
                                   TextColor="{StaticResource Gray500}"/>
                            <Label Text="{Binding CookTimeMinutes, StringFormat='⏱ {0} min'}"
                                   FontSize="12"
                                   TextColor="{StaticResource Gray400}"/>
                            <Label Text="{Binding Category, StringFormat='📂 {0}'}"
                                   FontSize="12"
                                   TextColor="{StaticResource Gray400}"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
